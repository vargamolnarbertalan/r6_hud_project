# R6 HUD Database Configuration
# Copy this file to .env and fill in your values

# MySQL Database Connection (all required for readiness scan)
DB_HOST=host-url
DB_PORT=3306
DB_USER=some-user
DB_PASSWORD=yourpassword
DB_NAME=dbname

# VDO.ninja parameters appended to live player view URLs in match config
# Example: &cover&cleanoutput&mutespeaker
# Leave empty if not needed, but the variable should exist in .env
VIDEO_PARAMS=

# Open http://localhost:8083/readiness-scan after first start

